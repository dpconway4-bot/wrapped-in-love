node_modules/
sqlite.db
.env
.vercel

# Exclude large audio source files — audio is bundled into dist/public already
attached_assets/

# Keep dist/public for Vercel (pre-built, includes all audio assets)
!dist/
!dist/public/
